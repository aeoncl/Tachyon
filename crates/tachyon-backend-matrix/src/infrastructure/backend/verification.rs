//! Placeholders: the SAS-flow and identity-reset subtasks fill these in. They exist now so
//! that the field shape of `BackendSessionMatrix` is already final.

pub struct VerificationFlowMatrix;

pub struct PendingReset;
